{
  "name": "@wufufu770/d2d-core",
  "version": "0.1.0",
  "description": "d2d 核心域函数薄包装: 转发 plugin/pentest-dsh/domain/* (scope 门控/环容量/经验沉淀/安全URL/分诊)",
  "type": "module",
  "license": "MIT",
  "private": true,
  "publishConfig": {
    "comment": "源码包: ESM 相对引用仓库内 ../../plugin/pentest-dsh/domain/*, 直接 npm publish 后路径无效。发布必须走 scripts/pack/inline-build.mjs 生成的 dist 产物(内联依赖源码并改写 import), 不要对源码包直接 publish。"
  },
  "main": "./src/index.mjs",
  "exports": {
    ".": "./src/index.mjs",
    "./package.json": "./package.json"
  },
  "files": [
    "src",
    "vendor",
    "README.md"
  ],
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
